# 本地开发：复制为 .env 即可（.env 已被 .gitignore 忽略，不会提交）
# Vercel 线上：在项目 Settings -> Environment Variables 中逐项添加同名变量

# ---- AI（渐进式增强：无 Key 自动 Mock，有 Key 走真实 AI）----
# OpenRouter Key（sk-or-v1-...），与 DEEPSEEK_API_KEY 二选一或都配（OPENROUTER 优先）
OPENROUTER_API_KEY=
# DeepSeek 官方 Key（sk-...），设置页填了也行，环境变量优先于设置页
DEEPSEEK_API_KEY=
# 默认模型（不填则 deepseek/deepseek-chat；设置页可再覆盖）
AI_MODEL=deepseek/deepseek-chat
# 线上访问地址（给 OpenRouter HTTP-Referer 排行榜归属；本地可留空）
APP_URL=http://localhost:3000
# 调试用：=1 时即使有 Key 也强制 Mock（正常不用加）
# FORCE_MOCK=

# ---- 存储（Vercel KV：线上持久化用；本地开发不需要）----
# 在 Vercel 后台 Storage -> Create Database -> KV，绑定项目后自动注入以下三个变量，
# 本地无需手填；KV 未绑定时线上自动降级为内存存储（实例回收即丢，仅演示）。
KV_REST_API_URL=
KV_REST_API_TOKEN=
KV_REST_API_READ_ONLY_TOKEN=
